 
  <div class="row">
      	<div class="col-lg-12 col-md-12 col-sm-12">
  			<div> 
                  
                   <div class="ficon">
            <form action="/updateNav" method="POST">
              <h1>Your Current Echoes</h1>
              <div class="form-group">
              <select data-placeholder="" class="form-control" name="oldCategoryID" style="width:350px;">
                <option  value="">Select New Category</option>
                {{#each userInterest}}

                <option value="{{this.categoryID}}">{{this.categoryName}}</option>
                

                {{/each}}
              </select>
              </div>
              <h1>Replacing Echoes</h1>
              <div class="form-group">
              <select data-placeholder="" class="form-control"  name="newCategoryID" style="width:350px;">
                <option  value="">Remove Category</option>
                {{#each allCategory}}

                <option value="{{this.categoryID}}">{{this.categoryName}}</option>
                

                {{/each}}
              </select>

</div>


              <br><br>
              <button type="submit" class="btn btn-lg btn-default" >Update Echoes</button>
              </form>
            </div>
                  
                  	{{!--this is the problem--}}
            <div class="textGrouped">
            </div>
  		</div>
      	
        </div>